peace_propaganda_action =  {
	category = covert

	require_acceptance = no
	is_visible = {
        has_idea_group = spy_ideas
	}
	
	is_allowed = {
        full_idea_group = spy_ideas
        FROM = {
            war_with = ROOT
        }
		has_spy_network_in = {
			who = FROM
			value = 50
		}
		NOT = { FROM = { has_country_modifier = active_propaganda } }
		NOT = { FROM = { full_idea_group = propaganda0 } }
	}
	
	on_accept = {
        FROM = { country_event = { id = mechanic_peace_propaganda_event.1 } }
		add_spy_network_in = {
			who = FROM
			value = -50
		}		
	}
	
	ai_will_do = {
		always = no
	}
}
